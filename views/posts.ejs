<head>
  <link rel="stylesheet" href="/css/posts.css" />
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous"
  />
  <%- include('partials/header') %>
  <style>
    .dropdown-content > a {
      display: none !important;
    }
  </style>
</head>
<% if (registeredName) { %>
<h2 id="welcome" style="text-align: center">
  Hi <%= registeredName %>, welcome to your posts!
</h2>
<% } %>

<div class="outer">
  <div class="container">
    <a href="/new"><button class="btn btn-success">New Post</button></a>
    <hr />
    <% posts.forEach(post => { %>
    <div class="outer">
      <h2 style="text-align: right"><%= post.title %></h2>
      <p style="text-align: right"><%= post.date %> | <%= post.time %></p>
      <p style="text-align: left"><%= post.content %></p>
      <p>Author: <%= registeredName %></p>
      <a href="/edit/<%= post.id %>"
        ><button class="btn btn-primary">Edit</button></a
      >
      <a
        href="/delete/<%= post.id %>"
        onclick="return confirm('Are you sure?');"
      >
        <button class="btn btn-danger">Delete</button>
      </a>
      <hr />
    </div>
    <% }); %>
  </div>
</div>
<%- include('partials/footer.ejs') %>

<!-- CRUD
Client presses New Post button, 
Client rerouted/displayed with a default tempelate form of title, topic, content, author 
(author being passed as Author: "registeredName JS var" //the logic is there for variable
Client submits, db updated...and front end displays the typed contexts
Client is able to view posts, with edit/delete button below it...
when Edit is pressed, client is able to update title, topic, content...Author is permanently integrated with code
when Delete is pressed, there should be a "Are you sure"....confirmation kind of logic before permanently retracting it from db

-->
